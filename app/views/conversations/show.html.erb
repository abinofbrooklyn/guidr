<h1>
  <%= conversation.subject %>
  <ul>
    <%= content_tag_for(:li, conversation.receipts_for(current_user)) do |receipt| %>
      <% message = receipt.message %>
      <h3>
        <%= message.sender.email %>
      </h3>
      <h3>
        <%= message.subject %>
      </h3>
      <p>
        <%= message.body %>
      </p>
    <% end %>
  </ul>
  <%= render "messages", conversation: conversation %>
</h1>
